# Ошибки:
# 1. Неверный алгоритм проверки (только длина и префикс).
# 2. Нет обработки регистра.
# 3. Допускает спецсимволы.
def validate_iban(iban):
    return iban.startswith("RU") and len(iban) == 20